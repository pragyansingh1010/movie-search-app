# Maintenance Notes

- Show loading feedback while search results are being retrieved.
- Handle empty searches without unnecessary requests.
- Provide a clear empty-result state.
- Keep movie cards readable when fields are missing.